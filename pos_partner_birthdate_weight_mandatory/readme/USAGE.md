This module functions only when the "Age Warning" setting is configured.

A popup will notify the cashier that the customer's weight is required based on
their date of birth, as determined by the Age Warning setting.
